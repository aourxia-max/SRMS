# Task010 押金与退租验收记录

## 2026-07-27 本机 TEST 数据补充核验

- 退租前置合同状态已核验：`TEST-HT-20260727224941`（合同 ID 7）自动进入 `ACTIVE`，对应房源 ID 4 自动成为 `RENTED`，可作为后续退租结算测试的正式状态起点。
- 尚未执行实际退租、押金退款或房源释放；这些结果仍保持待手工验收状态。

## 2026-07-27 退租结算、押金退款与重提闭环核验

- 使用 `TEST-HT-20260727224941`（合同 ID 7、房源 ID 4）登记测试押金 1,000 元；押金余额独立于租金账单实收。
- 首次结算审批发现欠租项目遗漏后，超级管理员驳回原结算单；管理员将原结算单 ID 1 退回草稿，保留原记录并补充后重新提交。该流程不新建结算单，合同和房源始终维持 `PENDING_CHECKOUT` 直至后续确认。
- 已确认结算：第 1 期账单欠租 500 元由押金抵扣，验房维修扣款 200 元（验收引用 `TEST-INSPECT-20260727`）由押金抵扣；押金余额和锁定应退金额均为 300 元，待补交金额为 0 元。
- 结算确认后，实际退房日之后的第 2、3 期未收账单均变更为 `VOIDED`；未在发起退租时提前作废。
- 已登记并确认押金退款 ID 1，退款金额 300 元、银行转账方式；退款凭证 `DEPOSIT_REFUND_PROOF` 已锁定，押金余额归零，合同状态为 `ENDED`，房源状态为 `EMPTY`，房态历史记录业务类型为 `DEPOSIT_REFUND`。
- 修复退款记录查询中凭证文件大小 `BigInt` 的响应序列化问题；接口可返回锁定时间和以字符串表示的文件大小，不会因已上传凭证返回 500。

## 已实现

- 押金收取以独立 `DEPOSIT` 收款和 `deposit_transactions` 正向余额流水保存，不进入租金账单分配或租金实收。
- 发起退租后合同和房源进入 `PENDING_CHECKOUT`；未来账单不在发起时删除或作废。
- 管理员提交结算时录入实际退房、验房、欠租及验房扣款。欠租必须关联有效账单；维修、损坏、清洁和其他扣款必须填写验收记录引用。
- 超级管理员确认结算后，按欠租优先、验房扣款其次抵扣押金，写入不可覆盖的押金流水，并作废实际退房日之后未收款的未来账单。
- 无应退押金、无待补交且预收款余额为零时，结算确认后直接结束合同并将房态更新为结算单锁定的目标状态。
- 管理员登记押金退款，必须上传 `DEPOSIT_REFUND_PROOF` 凭证；超级管理员确认后锁定凭证、写负向押金流水、完成结算、结束合同并更新房态。
- 前端入口：`/checkout`，提供押金收取、发起退租、结算项目、结算确认、凭证上传和退款确认入口。

## 数据库

- 已应用 `20260723150000_task010_deposits_checkout`；当前 MySQL 共 13 个 migration。

## 验证

- `npm run build` 通过。
- `npm run lint` 通过。
- `npm run test` 通过：10 个测试套件、30 个测试。
- `npm --prefix backend run test:e2e -- --runInBand` 通过：1 个测试套件、6 个接口测试。
- Prisma schema 校验通过；后端健康检查返回 200，未登录退租接口返回 401。

## 待手工验收

1. 用真实合同登记押金，核对押金余额不进入租金实收。
2. 分别验证押金全部抵扣、存在应退押金、存在待补交、存在预收款余额的结算分支。
3. 验证实际退款上传凭证后，超级管理员确认退款会锁定凭证并结束合同。

## 已知边界

- 预收款余额在结算中被显式阻止结束合同；其退款或经批准处理的独立登记入口需在后续预收款处理完善后接入，避免凭空生成资金流水。
