# Task012 驾驶舱验收记录

## 2026-07-27 本机 TEST 数据补充核验

- 超级管理员访问驾驶舱汇总：共 6 间房源，其中空置 2 间、已出租 3 间；欠租总额字段可返回给超级管理员。
- 以楼栋 ID 3 和房态 `EMPTY` 筛选后，房态图仅返回 1 间空置房，确认楼栋与房态筛选均在后端生效。
- 测试数据下仍可返回 7 天应缴提醒和待审批事项，驾驶舱未因已完成的退租测试合同而丢失其他经营待办。

## 2026-07-28 房态图独立楼栋筛选

- “楼栋房态图”卡片新增独立楼栋下拉筛选；它与卡片内房态多选组合使用，仅更新房态图房间格与图例数量。
- 全局经营指标、催租、欠租、合同到期和待办事项继续读取全局驾驶舱数据，不因卡片内楼栋筛选而改变。
- 使用本机 TEST 数据通过接口核对：测试 1 号楼（`TEST-B1`）返回 120 套空置房源；测试 2 号楼（`TEST-B2`）返回 72 套空置房源。
- 前端生产构建已通过；待在浏览器页面手工选择“测试 1 号楼 / 测试 2 号楼 / 全部楼栋”，核对房态图显示 120 / 72 / 全部房源。

## 2026-07-28 本月租金收缴概览

- 原“租金趋势”已替换为“本月租金收缴概览”，明确展示账期经营口径下的本月应收、已收、未收、逾期欠租和收缴率。
- 收缴率复用财务中心口径：有效实收 ÷ 租金净应收；本月净应收为零时显示“-”。
- 完整金额与收缴率仅对超级管理员返回和展示；管理员、游客仅见待收与逾期笔数提示。
- 本机自动化验证：自然月范围和闰年二月边界单元测试通过；前后端构建和后端静态检查通过。

## 2026-07-28 房源 360°详情页

- 驾驶舱楼栋房态图点击具体房间进入 `/properties/:id` 只读详情页。
- 页面包含房屋现状、业主资料、当前/最近合同、租户资料、风险提示，以及默认折叠的房态变更和历史合同。
- 超级管理员详情接口返回账单、收款、预收款和退款等财务数据；管理员单元测试确认响应不含 `financial` 字段。
- 本机接口核验：已出租测试房源返回 1 份合同、风险“有逾期账单”、5 条账单和 2 条收款记录。
- 房源管理列表新增“查看详情”入口，与驾驶舱房态图共用同一只读详情页。
- 从房源详情页点击“收款登记”时，有当前合同的房源自动带入合同并加载账单和收款记录；无合同房源保留普通收款入口。
- 从房源详情页点击“合同管理”时，合同列表按该房源筛选，并为新建合同预选该房源。

## 已实现

- 登录后首页为驾驶舱，采用统一的飞书式浅色、高信息密度界面。
- 支持楼栋与房态多选筛选，显示全部房态数量、房源总数、可经营房源、已出租数和出租率。
- 显示未来 7 天催租、逾期欠租、未来 30 天合同到期和待审批事项。
- 房态图按楼栋、楼层、房号排序，实时展示房间状态。
- 欠租金额汇总仅返回给超级管理员；其他角色仅可见其工作所需的欠租待办数量与明细。
- 首页提供收款登记和合同管理快捷入口。

## UI 基线

- 全局 Element Plus 使用中文 locale，日期/时间选择器显示中文。
- 卡片、表格、表单、按钮和页面内容区已统一为飞书式简洁专业风格。
- 保持高信息密度、少点击、权限清晰和危险操作显式提示的交互原则。

## 验证

- `npm run build`、`npm run lint`、`npm run db:validate` 通过。
- `npm run test` 通过：10 个测试套件、30 个测试。
- `npm --prefix backend run test:e2e -- --runInBand` 通过：1 个测试套件、6 个接口测试。
- 后端健康检查返回 200；未登录访问 `/api/dashboard` 返回 401。

## 待手工验收

1. 录入不同房态，验证楼栋筛选、状态多选和房态图排序。
2. 录入临近应缴、逾期账单和临近到期合同，核对各待办。
3. 分别使用超级管理员、管理员和游客账号验证欠租金额展示边界。
