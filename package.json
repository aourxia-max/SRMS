{
  "name": "smart-rental-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "SRMS 房屋租赁管理系统",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "concurrently -n WEB,API -c cyan,green \"npm --prefix frontend run dev\" \"npm --prefix backend run start:dev\"",
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "test": "npm --prefix backend run test -- --runInBand",
    "lint": "npm --prefix backend run lint:check",
    "db:generate": "npm --prefix backend run prisma:generate",
    "db:validate": "npm --prefix backend run prisma:validate"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
